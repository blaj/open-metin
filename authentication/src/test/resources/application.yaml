spring:
  profiles:
    active: test

  datasource:
    url: jdbc:h2:mem:testdb
    driver-class-name: org.h2.Driver

  jpa:
    hibernate:
      ddl-auto: update

  liquibase:
    enabled: false

  sql:
    init:
      platform: test

open-metin:
  tcp:
    host: 0.0.0.0
    port: 11000
    idle-seconds: 60